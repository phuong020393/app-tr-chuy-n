<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Chat SaaS Demo</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      display: flex;
      height: 100vh;
    }
    .sidebar {
      width: 260px;
      background: #1e293b;
      color: white;
      padding: 20px;
    }
    .sidebar h2 {
      margin-top: 0;
    }
    .sidebar button {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      background: #38bdf8;
      border: none;
      cursor: pointer;
      font-weight: bold;
    }
    .main {
      flex: 1;
      display: flex;
      flex-direction: column;
    }
    .chat-header {
      padding: 15px;
      background: #0f172a;
      color: white;
    }
    .chat-body {
      flex: 1;
      padding: 20px;
      overflow-y: auto;
    }
    .message {
      margin-bottom: 10px;
    }
    .me {
      text-align: right;
      color: #2563eb;
    }
    .system {
      color: #64748b;
      font-size: 12px;
    }
    .chat-input {
      display: flex;
      padding: 10px;
      background: white;
    }
    .chat-input input {
      flex: 1;
      padding: 10px;
      font-size: 16px;
    }
    .chat-input button {
      padding: 10px 20px;
      margin-left: 10px;
      background: #22c55e;
      border: none;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }
    .coin {
      margin-top: 20px;
      font-size: 14px;
    }
  </style>
</head>
<body>

<div class="sidebar">
  <h2>👤 User</h2>
  <p>ID: <b id="userId"></b></p>
  <div class="coin">💰 Coin: <b id="coin">100</b></div>

  <button onclick="sendGift()">🎁 Send Gift (10 coin)</button>
  <button onclick="addCoin()">➕ Nạp Coin</button>
</div>

<div class="main">
  <div class="chat-header">
    💬 Chat Room (Demo)
  </div>
  <div class="chat-body" id="chatBody">
    <div class="message system">System: Welcome to Chat SaaS Demo</div>
  </div>
  <div class="chat-input">
    <input id="messageInput" placeholder="Nhập tin nhắn..." />
    <button onclick="sendMessage()">Send</button>
  </div>
</div>

<script>
  const userId = "user_" + Math.floor(Math.random() * 1000);
  let coin = 100;

  document.getElementById("userId").innerText = userId;

  function renderMessage(text, cls = "") {
    const div = document.createElement("div");
    div.className = "message " + cls;
    div.innerText = text;
    document.getElementById("chatBody").appendChild(div);
    div.scrollIntoView();
  }

  function sendMessage() {
    const input = document.getElementById("messageInput");
    if (!input.value) return;
    renderMessage(userId + ": " + input.value, "me");
    input.value = "";
  }

  function sendGift() {
    if (coin < 10) {
      alert("Không đủ coin");
      return;
    }
    coin -= 10;
    document.getElementById("coin").innerText = coin;
    renderMessage("🎁 " + userId + " sent a gift!", "system");
  }

  function addCoin() {
    coin += 50;
    document.getElementById("coin").innerText = coin;
    renderMessage("💳 Coin added (+50)", "system");
  }
</script>

</body>
</html>
